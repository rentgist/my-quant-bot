# TASK-001: Historical bootstrap context

This file preserves historical context for the early AI Company OS bootstrap exploration. It is inactive documentation, not an executable task, queue record, lifecycle record, or instruction to a worker.

The bootstrap explored role definitions, planning artifacts, handoffs, and review concepts. Pull Request #62 is historical and superseded because it also included a duplicate lightweight Python worker experiment. Current work must neither merge nor depend on that bootstrap branch.

The mature PowerShell control plane on `main`, centered on `scripts/codex-queue-worker.ps1`, is the only production execution engine. Current tasks must be represented by their GitHub Issues and existing lifecycle flow.

Any locally reported `PROJECT_MAP`, handoff, review, or similar bootstrap artifact is not durable GitHub evidence unless it is explicitly imported into the current operational record and independently verified against the Issue, PR diff, tests, review evidence, and CI.
